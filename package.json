{
	"private": true,
	"version": "1.0.0",
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"lerna": "^4.0.0",
		"prettier": "^2.2.1"
	},
	"scripts": {
		"build": "yarn workspaces foreach run build",
		"start": "yarn workspace @compass-surveys/server start"
	},
	"dependencies": {
		"typescript": "^4.2.3"
	}
}
